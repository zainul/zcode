[package]
name = "zcode-benches"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
domain = { path = "../crates/domain", version = "0.2.0" }
criterion = "0.5"

[[bench]]
name = "smoke"
harness = false

[lib]
bench = false
