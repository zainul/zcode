Points at `fake-provider.py --lines 60`, which answers with sixty numbered
lines. Sixty rows do not fit in a 32-row terminal, so the pane has something to
scroll and every row can be identified — "is `line 001` on screen?" is a
question with an answer, where "did it scroll?" is not.
