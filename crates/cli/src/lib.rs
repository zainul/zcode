//! QAgent — the lean Rust coding agent.
//!
//! This crate is the composition root: it wires concrete infrastructure
//! adapters into the `app::App` orchestrator and exposes the CLI entry point.

pub mod cli;
