[package]
name = "infra-filesystem"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
domain = { path = "../../domain", version = "0.4.0" }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3.10"
