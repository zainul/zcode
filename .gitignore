/target
**/*.rs.bk
.env
.qagent_node_config.env
zcode.toml.local
zcode.json.local
.coverage/
*.profraw
/logs/
node.json

# Runtime state: sessions, reports, and skills zcode writes as it runs. This
# repo is also a working directory for zcode itself, so the root has one too.
/.zcode/

# Acceptance-harness scratch
examples/**/.zcode/
examples/__pycache__/
examples/demo-go/demo
